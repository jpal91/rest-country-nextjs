import colors from "../base/colors";

const appbar = {
  styleOverrides: {
    root: {
      display: "flex",
      width: "100%",
      bgColor: colors.primary.white
    },
    colorDefault: colors.primary.white
  }
};

export default appbar;
