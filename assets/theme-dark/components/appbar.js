import colors from "../base/colors";

const appbar = {
  styleOverrides: {
    root: {
      display: "flex",
      width: "100%",
      backgroundColor: colors.primary.main
    },
  }
};

export default appbar;
